import path from "node:path";

export const PATH_FILE = path.join(process.cwd(), "files", "SD_test2_dd.img");

export const PATH_XML = path.join(process.cwd(), "files", "data.xml");

export const PATH_TEXT_EJ = path.join(
  process.cwd(),
  "files",
  "SD_test2_EJ.txt"
);

export const PATH_TEXT_JSON = path.join(
  process.cwd(),
  "files",
  "SD_test2_EJ.json"
);

export const PATH_TEXT_PDF = path.join(
  process.cwd(),
  "files",
  "SD_test2_EJ.pdf"
);

export const PATH_FOLDER = path.join(process.cwd(), "data");
